---
import Badge from './Badge.astro';
import Button from './Button.astro';

interface Props {
  title: string;
  description: string;
  tags: string[];
  github?: string;
  demo?: string;
  image?: string;
  lang?: 'pt' | 'en';
}

const { title, description, tags, github, demo, image, lang = 'pt' } = Astro.props;
const visitLabel = lang === 'pt' ? 'Visitar' : 'Visit';
const codeLabel = lang === 'pt' ? 'Código' : 'Code';
---

<div class="bg-surface-1 border border-hairline rounded-lg p-6 hover:bg-surface-2 transition-all duration-300 space-y-4">
  {image && (
    <div class="relative overflow-hidden rounded-md group">
      <img
        src={image}
        alt={title}
        class="w-full h-64 object-cover rounded-md group-hover:scale-105 transition-transform duration-300"
      />
    </div>
  )}

  <div class="space-y-3">
    <h3 class="text-card-title font-semibold text-ink">{title}</h3>
    <p class="text-body text-ink-muted">{description}</p>
  </div>

  <div class="flex flex-wrap gap-2">
    {tags.map((tag) => (
      <Badge text={tag} variant="secondary" />
    ))}
  </div>

  <div class="flex gap-3 pt-2">
    {demo && (
      <Button href={demo} variant="primary" target="_blank">
        {visitLabel}
      </Button>
    )}
    {github && (
      <Button href={github} variant="secondary" target="_blank">
        {codeLabel}
      </Button>
    )}
  </div>
</div>
