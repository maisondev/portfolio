---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Experience from '@/components/sections/Experience.astro';
import { getLanguageFromUrl, getTranslation, type Language } from '@/i18n/utils';
import { getStaticPaths } from '@/i18n/routes';

export { getStaticPaths };

const lang = getLanguageFromUrl(Astro.url) as Language;
const t = (key: string) => getTranslation(lang, key);
---

<BaseLayout
  title={`${t('pages.experience.title')} - Maison Galvão`}
  description={t('pages.experience.description')}
>
  <div class="max-w-5xl mx-auto px-6 md:px-8 py-12 md:py-16 space-y-4">
    <h1 class="text-5xl sm:text-6xl font-bold text-ink tracking-tight">{t('pages.experience.title')}</h1>
    <p class="text-body-lg text-ink-muted max-w-3xl">
      {t('pages.experience.description')}
    </p>
  </div>

  <Experience id="experience" lang={lang} />
</BaseLayout>
